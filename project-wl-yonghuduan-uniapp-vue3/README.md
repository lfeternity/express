# SL Express 用户端

基于 Uni-app 和 Vue 3 的寄件用户端，覆盖用户登录、地址簿、寄件下单、订单支付和物流轨迹查询等功能。

## 开发与运行

建议使用 HBuilderX 打开当前目录，选择运行到浏览器、模拟器或真机。发布微信小程序前，请在 `manifest.json` 和平台后台配置自己的 App ID；接口请求默认通过网关的 `/customer` 路由接入。

安装依赖（如使用命令行）：

```bash
yarn install
```

请使用自己的测试账号和服务配置，不要提交本地密钥、平台签名或构建产物。
