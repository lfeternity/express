# SL Express 司机端

基于 Uni-app 和 Vue 3 的司机运输作业端，覆盖运输任务、提货、在途上报、异常上报和到达确认等流程。

## 开发与运行

建议使用 HBuilderX 打开当前目录，选择运行到浏览器、模拟器或真机。发布前请在 `manifest.json` 配置自己的应用信息，并确认接口地址指向 SL Express 网关的 `/driver` 路由。

安装依赖（如使用命令行）：

```bash
yarn install
```

请使用自己的测试账号、地图配置和服务地址，不要提交本地密钥或构建产物。
